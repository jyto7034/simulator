# Experiments: Unity Wire DTO Boundary Cleanup

| Date | Attempt | Result | Notes |
| --- | --- | --- | --- |
| 2026-07-06 | Created goal documents from confirmed policy decision for item 5. | Planned. | Implementation not started in this entry. This goal is intentionally standalone because DTO shape changes can affect Unity docs, probes, and client code. |
| 2026-07-07 | Inventory core/Unity wire surfaces for `BattleEventLogEntry`, `CombatPreview`, `NodeSession`, and `Debug` formatting. | Success. | Confirmed live `battle_update.events_delta.events` used `BattleEventLogEntry` directly. Confirmed `CombatPreview` is actively consumed by Unity as pre-battle/preview data, so a broad split would require coordination. |
| 2026-07-07 | Add explicit live presentation event DTO boundary. | Success. | Added `LiveBattlePresentationEventDto` and converted internal event log entries at `ActiveBattleSession::battle_update_dto_from_events`; JSON shape is preserved. |
| 2026-07-07 | Replace wire-visible `format!("{:?}")` snapshot strings with explicit mapping helpers. | Success. | Updated inventory/roster/trust/battle-tier snapshot labels and added focused tests for the explicit contract values. |
| 2026-07-07 | Full lib validation after removing Debug-formatted snapshot error message. | Failed, then fixed. | `cargo test -p game_core --lib -- --test-threads=1` initially failed because `employee_roster_snapshot_surfaces_effective_profile_errors` still expected the old `InventoryItemNotFound` Debug string in `message`. Updated the test to assert the stable `code` plus the new explicit human message, then reran the focused test and full lib test successfully. |
| 2026-07-07 | Validate focused DTO, combat preview, live combat flow, live RON, and broad lib tests. | Success. | `cargo fmt --check`, `cargo check -p game_core`, focused presentation/string-label tests, `combat_preview`, `game::world::tests::combat`, `--test ron_loading`, `--test live_item_skill_activation`, and full `cargo test -p game_core --lib -- --test-threads=1` passed. The goal example command with an extra `live_item_skill_activation` filter ran 0 tests, so the integration test was rerun without the filter and passed 3/3. |
| 2026-07-07 | Re-run completion audit validation from current worktree. | Failed, then fixed by rerun discipline. | Focused DTO tests, `combat_preview`, live RON, live item skill activation, `cargo fmt --check`, `cargo check -p game_core`, and full `cargo test -p game_core --lib -- --test-threads=1` passed. A parallel run of `game::world::tests::combat` failed once because it overlapped with the full lib run and parsed a concurrently-written battle record JSON while it was incomplete (`EOF while parsing a string`). Re-ran `cargo test -p game_core game::world::tests::combat --lib -- --test-threads=1` by itself and it passed 39/39. |
