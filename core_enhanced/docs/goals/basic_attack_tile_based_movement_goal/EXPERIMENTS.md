# Basic Attack Tile-Based Movement Goal Experiments

| Date | Attempt | Result | Follow-up |
| --- | --- | --- | --- |
| 2026-07-03 | Goal setup and initial audit | Created the goal after auditing `movement/planner.rs`, `targeting.rs`, and current canonical range policy. Confirmed final attack eligibility is tile-based, while movement planning still uses `basic_attack.range_units` through `MovementGoal::AttackUnit.desired_range` and `closest_enemy_in_attack_range()`. | Start implementation with a focused inventory of movement goal construction and tests that prove old continuous stop behavior. |
| 2026-07-03 | Runtime inventory | Re-read `planner.rs`, `steering.rs`, `engine.rs`, `basic_attack.rs`, `targeting.rs`, and `blocking.rs`. `EnemyMovementPlan` currently exposes `PathAlongCells` only, so no route-less boss/special path was reached in this pass. The remaining basic-attack continuous stop path is: planner emits `MovementGoal::AttackUnit { desired_range: basic_attack.range_units }`, steering uses `desired_range` for target approach/reached checks, and `closest_enemy_in_attack_range()` uses `UnitBody::can_reach()` with the same value. `build_continuous_movement_input()` also copies `basic_attack.range_units` into `MovementUnitInput.attack_range_units`, but focused search shows that field is not read by the direct movement engine. | Add tests that fail when huge `range_units` creates an attack stop goal outside tile range, then replace basic-attack movement acquisition with tile-range acquisition and remove `desired_range` from the basic attack goal semantics. |
| 2026-07-03 | First code pass and focused test | Removed `MovementGoal::AttackUnit.desired_range`, removed `MovementUnitInput.attack_range_units`, and changed planner acquisition to use tile-based target APIs. `cargo test -p game_core movement::planner --lib -- --test-threads=1` failed at compile time because planner tests still matched the removed `approach_point` field. | Update old test patterns to the new `AttackUnit { target_id }` shape, then rerun focused tests. |
| 2026-07-03 | Planner test repair | Updated planner test patterns, added regression coverage for large `range_units` outside tile range, and reran `cargo test -p game_core movement::planner --lib -- --test-threads=1`. | Passed: 25 planner tests. Continue with broader movement/targeting validation and docs sync. |
| 2026-07-03 | Movement validation | Ran `rg` for `basic_attack.range_units`, `desired_range`, `attack_range_units`, `closest_enemy_in_attack_range`, and `can_reach` in movement/basic-attack/targeting code. Remaining `range_units` hits were test fixture setup, the continuous `UnitBody::can_reach()` helper, and a public `basic_attack_range_units()` helper in targeting. Ran `cargo test -p game_core movement --lib -- --test-threads=1`: passed 68 tests. A later attempted cargo command with multiple test filters failed because Cargo accepts only one positional test filter. | Remove the unused `basic_attack_range_units()` helper, rerun the tile-range focused tests with valid filters, then run broad check. |
| 2026-07-03 | Targeting helper cleanup and focused validation | Removed the unused `basic_attack_range_units()` helper so targeting no longer exposes a generic basic-attack continuous range accessor. Reran valid focused filters: `cargo test -p game_core range_units --lib -- --test-threads=1` and `cargo test -p game_core basic_attack --lib -- --test-threads=1`. | Passed: 5 `range_units` tests and 31 `basic_attack` tests. Update canonical docs and run final broad validation. |
| 2026-07-03 | Docs sync and final validation | Updated `game_rulebook.md`, `skill_target_contract.md`, and this goal's plan/notes to state that basic-attack stop/acquisition is tile-based and not `range_units`-based. Ran `cargo fmt`, `cargo test -p game_core movement --lib -- --test-threads=1`, `cargo test -p game_core range_units --lib -- --test-threads=1`, `cargo test -p game_core basic_attack --lib -- --test-threads=1`, and `cargo check -p game_core`. | Passed. No Unity-facing DTO change. |

## Verification Log

- `cargo test -p game_core movement::planner --lib -- --test-threads=1` passed after planner test repair.
- `cargo test -p game_core movement --lib -- --test-threads=1` passed after movement goal semantics repair.
- `cargo test -p game_core basic_attack_uses_tile_range_not_range_units fixed_defense_player_basic_attack_uses_facing_tile_pattern_not_continuous_radius skill_enemy_single_uses_tile_range_not_range_units --lib -- --test-threads=1` failed because Cargo accepts only one positional test filter. This was a command issue, not a code/test failure.
- `cargo test -p game_core range_units --lib -- --test-threads=1` passed after replacing the invalid multi-filter command.
- `cargo test -p game_core basic_attack --lib -- --test-threads=1` passed after replacing the invalid multi-filter command.
- `cargo fmt` completed successfully.
- `cargo test -p game_core movement --lib -- --test-threads=1` passed again after docs sync and formatting.
- `cargo test -p game_core range_units --lib -- --test-threads=1` passed again after docs sync and formatting.
- `cargo test -p game_core basic_attack --lib -- --test-threads=1` passed again after docs sync and formatting.
- `cargo check -p game_core` passed.
