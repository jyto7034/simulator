# Experiments: Event Preview Test Artifact Cleanup

| Date | Attempt | Result | Notes |
| --- | --- | --- | --- |
| 2026-07-06 | Created goal documents from confirmed policy decisions for items 4, 6, 7, and 8. | Planned. | Implementation not started in this entry. First implementation step must inventory current runtime code and live RON because earlier cleanup goals may already cover part of this scope. |
| 2026-07-06 | Inventory runtime code, live RON, debug paths, and test-file references. | Success. | Confirmed `map_content.rs` still had first-event fallback, live `node_definitions.ron` had `Event(event_id: None)`, `enemy_stat_scale` was `serde(skip)` but undocumented as internal, debug export code already writes under `target/`, and `skill_refactor_validation.rs` was the only current refactor-era test filename. |
| 2026-07-06 | Remove Event node fallback and require explicit event ids. | Success. | Replaced runtime fallback with `InvalidStaticData`, added map definition validation for Event payload ids, changed live RON to `white_night_confession_01`, and updated map-flow tests to author explicit event ids. |
| 2026-07-06 | Clarify `CombatPreview.enemy_stat_scale` as internal state. | Success after one failed test fixture. | Added runtime contract text and serde round-trip test. First test used `encounter_id: None` and failed with `combat preview requires an authored pve encounter`; fixed by building a tiny authored encounter fixture so the test isolates the scale serialization boundary. |
| 2026-07-06 | Quarantine test/debug artifact behavior. | Success. | Code paths already write to `target/debug_event_log_exports` and `target/battle_records`; added an integration test asserting the shared debug export helper points under `target/`. Existing tracked repo-root artifacts were not deleted in this goal. |
| 2026-07-06 | Rename refactor-era skill contract test. | Success. | Renamed `tests/skill_refactor_validation.rs` to `tests/skill_runtime_contract.rs` and updated active/current references. Historical completed goal docs were intentionally left unchanged unless they were current commands or active notes. |
| 2026-07-06 | Focused validation. | Success. | Passed `cargo test -p game_core event_node_definitions_require_explicit_event_id --lib -- --test-threads=1`, `cargo test -p game_core combat_preview_enemy_stat_scale_is_internal_not_wire_contract --lib -- --test-threads=1`, `cargo test -p game_core --test skill_runtime_contract debug_event_log_exports_are_quarantined_under_target -- --test-threads=1`, and `cargo test -p game_core active_boss_omen_chain_overlays_matching_event_node --lib -- --test-threads=1`. |
| 2026-07-06 | Final validation. | Success. | Passed `cargo fmt --check`, `cargo test -p game_core --test skill_runtime_contract -- --test-threads=1`, `cargo check -p game_core`, and `cargo test -p game_core --lib -- --test-threads=1` (602 tests). |
