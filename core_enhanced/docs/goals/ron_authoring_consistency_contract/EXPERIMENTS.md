# Experiments: RON Authoring Consistency Contract

| Date | Attempt | Result | Notes |
| --- | --- | --- | --- |
| 2026-07-06 | Created goal documents from confirmed policy decision for item 9. | Planned. | Implementation not started in this entry. This goal is standalone because schema strictness can touch many live RON domains and should be applied with careful inventory. |
| 2026-07-06 | Inventory live raw schemas, defaults, casing, and existing strict domains. | Success. | Confirmed `run_policy`, `skill_data`, `corroded_wave`, `boss_omen`, map generation/node definitions, and parts of reward/event/shop already had strict schema coverage. Found weaker live gameplay authoring boundaries in PvE encounters/waves, corroded employee profiles, shop database/pools, and event enum/container shapes. `docs/data_loading_contract.md` does not exist, so canonical implementation rules belong in `docs/core_runtime_contract.md`. |
| 2026-07-06 | Add strict authoring to selected core domains. | Success. | Added `deny_unknown_fields` to PvE encounter/wave nested schemas, corroded employee profile/range preset raw schemas, shop database/pool schemas, and event scene/choice/effect/database schemas. No legacy aliases were introduced. |
| 2026-07-06 | Add focused unknown/stale-field rejection tests. | Success. | Added tests for PvE top-level typo, PvE nested wave typo, corroded employee profile typo, shop pool typo, and event choice effect typo. Existing corroded wave stale `difficulty` and shop raw `hidden_items` rejection tests remain in place. |
| 2026-07-06 | Validate focused strictness tests. | Success. | Passed `cargo test -p game_core pve_encounter_rejects_unknown_authoring_fields --lib -- --test-threads=1`, `cargo test -p game_core pve_wave_rejects_unknown_nested_authoring_fields --lib -- --test-threads=1`, `cargo test -p game_core corroded_employee_database_rejects_unknown_authoring_fields --lib -- --test-threads=1`, `cargo test -p game_core shop_database_rejects_unknown_pool_authoring_fields --lib -- --test-threads=1`, and `cargo test -p game_core event_database_rejects_unknown_choice_effect_fields --lib -- --test-threads=1`. |
| 2026-07-06 | Validate live RON loading after stricter schemas. | Success. | Passed `cargo test -p game_core --test ron_loading -- --test-threads=1` (18 tests) and `cargo test -p game_core live_ron --lib -- --test-threads=1`. Current live data satisfies the stricter touched schemas. |
| 2026-07-06 | Document authoring contract. | Success. | Added a Live RON Authoring section to `docs/core_runtime_contract.md`, including unknown-field rejection, default policy, enum casing, string id casing, and strict domains. |
| 2026-07-06 | Run broad validation with the default Cargo target dir. | Failed. | `cargo test -p game_core --lib -- --test-threads=1` could not open `/mnt/f/work/simulator/target/debug/.cargo-lock` because the sandbox exposes the parent workspace target directory as read-only. This was an environment/write-boundary failure, not a test failure. |
| 2026-07-06 | Rerun broad validation with a writable target dir. | Success. | Passed `CARGO_TARGET_DIR=/tmp/core_enhanced_target cargo test -p game_core --lib -- --test-threads=1` (607 tests). Also passed `cargo test -p game_core validation --lib -- --test-threads=1`, `cargo check -p game_core`, and `cargo fmt --check`. |
