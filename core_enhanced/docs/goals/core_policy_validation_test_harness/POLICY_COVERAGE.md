# Policy Coverage Cross-Check

Date: 2026-06-22

Source policy file: `docs/goals/core_component_refactor_master/POLICY_DECISIONS.md`

## Coverage Summary

| Policy area | Confirmed policies | Implementation / validation evidence |
| --- | --- | --- |
| `world_run_progression` | `RUN_SYSTEM_POLICY data source`; `battle_records export` | `core_policy_static_data_map_scenario` moved run policy to live RON and validated loading. `core_policy_unity_server_contract` verified `battle_records/run_<seed>/*.json` remains always-on debug export and is asserted by combat tests. |
| `map_node` | `map authoring/generation policy source`; `NodeSessionKind`; `MapViewDto progression projection`; `map node tags` | `core_policy_static_data_map_scenario` made map generation policy data-driven, kept runtime/session state authoritative, migrated MapView progression to `MapNodeDto.state`, and removed tags from live node data. |
| `behavior_state_gate` | `allowed_actions source`; `action validation gates`; `RequestDeploymentRangePreview gate` | `core_policy_movement_battle_runtime` verified action gating and pre-dispatch validation gates; deployment range preview remains an allowed action gate with read-only result transport. |
| `movement_blocking` | `forced movement clears engagement`; `MovementStopped event`; `movement tick policy source`; `opponent spawn occupancy policy` | `core_policy_movement_battle_runtime` verified existing forced-movement/stop semantics, moved movement numeric policy to live run policy, and removed the nearest-open spawn fallback as unnecessary under overlap rules. |
| `battle_runtime` | `UnitWithdrawn timeline event`; `BehaviorResult boundary`; `battle runtime numeric policy`; `Timeline naming` | `core_policy_movement_battle_runtime` added `UnitWithdrawn` event and moved runtime numbers to live policy. `core_policy_unity_server_contract` added domain result contracts and preserved battle transport side-message mapping. Timeline naming remained as current append-only event log wording in code/docs. |
| `battlefield_scenario_wave` | `encounter-less combat preview and empty wave fallback`; `battlefield archetype random selection source`; `SplitRoom content policy`; `FacilityEntity future schema`; `authored defense route required` | `core_policy_static_data_map_scenario` added validation/fixtures for authored route requirements, server preview validation, archetype policy source, SplitRoom content boundary, and FacilityEntity schema direction. |
| `skill_targeting_range` | `explicit cast_targeting`; `remove range_units from skill targeting`; `TileArea affected_tiles clipping`; `long line and piercing skill representation`; `runtime range preview source` | `core_policy_skill_targeting_projectile` implemented explicit cast targeting, removed live `range_units`, clipped TileArea affected tiles to valid tiles, represented long/piercing lines through tile patterns plus clipping, and preserved runtime preview source behavior. |
| `basic_attack_projectile_judgement` | `projectile launch owner snapshot`; `projectile miss event source`; `same timestamp attack resolve batch`; `remove SplashClusterFirst targeting profile` | `core_policy_skill_targeting_projectile` and `core_policy_movement_battle_runtime` verified launch-owner snapshot damage context, canonical miss event, same-timestamp attack ordering, and removed the legacy `SplashClusterFirst` profile. |
| `damage_stats_entry` | `move speed stat source`; `move speed runtime update timing` | `core_policy_buff_status_stats` made `MoveSpeedUnitsPerMs` the runtime movement speed stat and fixed update timing to the next tick policy. |
| `buff_status_effect` | `hard CC action restriction source`; `hard CC replacement event`; `clear active buffs on death`; `non-periodic control status max stacks` | `core_policy_buff_status_stats` moved hard-CC behavior to active buff/effect state, emitted explicit replacement/expiration semantics, cleaned buffs on death, and validated `max_stacks == 1` for control statuses. |
| `employee_growth_trust` | `remove unused growth ids`; `trust feature surface`; `growth curve and XP policy source`; `remove legacy employee grade`; `trauma applies to final battle max HP` | `core_policy_employee_item_growth` removed unused growth ids and legacy grade, moved growth/XP policy to data, applied trauma to final battle max HP, and left trust feature surface intentionally deferred per confirmed policy. |
| `item_equipment_skill_fragment` | `remove automatic duplicate fragment conversion`; `skill fragment equip limit policy`; `skill fragment dismantle auto unequip`; `fragment dust resource source`; `bound equipment interaction lock` | `core_policy_employee_item_growth` removed automatic duplicate conversion, implemented fragment equip ownership/rarity limits, allowed dismantle with auto unequip, kept fragment dust as a resource, and enforced bound equipment interaction locks. |
| `reward_shop_support` | `canonical grant executor`; `atomic reward claim`; `reward granted fragment and research diff`; `reward experience target policy`; `enkephalin overflow policy`; `remove reward tags`; `explicit equipment reward pools`; `forbidden abnormality reward validation` | `core_policy_grant_economy_rewards` promoted the grant executor, made reward claim atomic, added reward diffs, applied reward XP targeting, checked Enkephalin overflow, removed reward tags, required explicit equipment pools, and moved forbidden abnormality grants to validation failure. |
| `data_ron_validation` | `starter employee loadout source`; `remove legacy random event RON`; `server startup live preview validation` | `core_policy_static_data_map_scenario` and `core_policy_grant_economy_rewards` moved starter equipment/fragments to live RON, removed/isolated legacy random event loading, and added server live preview validation. |
| `unity_server_contract` | `shared gameplay command DTO`; `typed run snapshot DTO`; `combat result timeline attachment ownership`; `remove dormant battle resync setup field` | `core_policy_unity_server_contract` merged gameplay command payloads onto core `PlayerBehavior`, typed `RunSnapshotDto` and nested sections, added `CombatResultTimelineAttachmentDto`, and removed `BattleResync.setup`. |
| `validation_test_harness` | `remove old timeline log validation acceptance`; `live content audit manifest` | This subgoal removed normal acceptance for timeline versions `6`/`7`, added focused regression coverage, moved live skill delivery/coverage rosters to `docs/audit/live_skill_catalog_manifest.ron`, and updated tests to consume the manifest. |

## Cross-Check Result

- No new policy-decision report was required during this subgoal.
- Trust-system work remains intentionally deferred and is not treated as incomplete implementation drift.
- Final broad validation still needs to run after this checklist.
